pipeline {
  agent any
  environment {
    SCANNER_HOME= tool 'sonar'
  }
  stages {
    stage('Static Code Analysis') {
      steps {
        sh '''
        echo "${SCANNER_HOME}"
        '''
        }
      }
      stage('Unit Test') {
         steps {
           sh 'python --version'
                 echo 'Unit testing'
         }
      }
    stage('build') {
      steps { 
             echo 'echo3'
      }
    } 
  }
}
