pipeline {
  agent any
  environment {
    SCANNER_HOME= tool 'sonar'
  }
  stages {
    stage('Static Code Analysis') {
      steps {
        sh '''
        echo "${SCANNER_HOME}"
        '''
        }
      }
      stage('Unit Test') {
         steps {

                 echo 'Unit testing'
                 sh 'cd /var/jenkins_home/workspace/DevOps_Academy_Project/cidr_convert_api/python/'
           sh 'pwd' 

           sh 'ls' 
           sh 'python3 api.py'
         }
      }
    stage('build') {
      steps { 
             echo 'echo3'
      }
    } 
  }
}
