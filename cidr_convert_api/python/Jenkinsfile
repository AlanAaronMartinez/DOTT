pipeline {
  agent any
  environment {
    SCANNER_HOME= tool 'sonar'
  }
  stages {
    stage('Static Code Analysis') {
      steps {
        sh '''
        echo "${SCANNER_HOME}"
        '''
        }
      }
      stage('Unit Test') {
         steps {

                 echo 'Unit testing'
                 sh 'cd /var/jenkins_home/workspace/DevOps_Academy_Project/cidr_convert_api/python/'

           sh '/var/jenkins_home/workspace/DevOps_Academy_Project/cidr_convert_api/python/api.py'
           sh 'ls'
           sh 'pwd'
         }
      }
    stage('build') {
      steps { 
             echo 'echo3'
      }
    } 
  }
}
